# Starter Buildings

Example source directory for the BlockForge Blueprint Pack v1 format.

To create a distributable pack, zip this directory so `blockforge-pack.json`
is at the zip root, then name the file:

```text
starter_buildings.blockforgepack.zip
```

The mod connectors load pack files from:

```text
config/blockforge/packs/
```
